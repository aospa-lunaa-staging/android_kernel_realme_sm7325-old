vendor := $(srctree)/$(src)

subdir-y += qcom

ifeq ($(CONFIG_DEVICE_DTS_LUNAA),y)
subdir-y += oplus/lunaa
endif

ifeq ($(CONFIG_DEVICE_DTS_CUPID),y)
subdir-y += oplus/cupid
endif

ifeq ($(CONFIG_DEVICE_DTS_LEMONADEV),y)
subdir-y += oplus/lemonadev
endif

ifeq ($(CONFIG_DEVICE_DTS_MARTINI),y)
subdir-y += oplus/martini
endif

ifeq ($(CONFIG_DEVICE_DTS_PORSCHE),y)
subdir-y += oplus/porsche
endif